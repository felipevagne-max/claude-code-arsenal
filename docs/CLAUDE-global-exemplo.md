# mentecopy
- **mentecopy** (`~/.claude/skills/mentecopy/SKILL.md`) - modo de pensamento copy DR black, liderado por Alen Sultanic. Trigger: `/mentecopy`
When the user types `/mentecopy`, invoke the Skill tool with `skill: "mentecopy"` before doing anything else.

# graphify
- **graphify** (`~/.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, invoke the Skill tool with `skill: "graphify"` before doing anything else.

# lint
- **lint** (`~/.claude/skills/lint/SKILL.md`) - audita o vault Obsidian completo. Trigger: `/lint`
When the user types `/lint`, invoke the Skill tool with `skill: "lint"` before doing anything else.

# raiox
- **raiox** (`~/.claude/skills/raiox/SKILL.md`) - diagnóstico de funil pela Máquina de Crença (funil = paciente, sintoma = doença). Trigger: `/raiox`
When the user types `/raiox`, invoke the Skill tool with `skill: "raiox"` before doing anything else.
